# ============================================================
# requirements.txt — Python packages for the Django backend
# Install with: pip install -r requirements.txt
# ============================================================

# Web framework
Django==4.2.7

# Django REST Framework — for building JSON APIs
djangorestframework==3.14.0

# CORS headers — allow React frontend to call this API
django-cors-headers==4.3.1

# Environment variables (.env file support)
python-dotenv==1.0.0

# Selenium — browser automation for LeetCode
selenium==4.15.2

# Undetected ChromeDriver — bypasses Cloudflare/bot detection on LeetCode
undetected-chromedriver==3.5.5

# setuptools — provides distutils (required by undetected-chromedriver on Python 3.13+)
setuptools>=70.0.0

# WebDriver manager — auto-downloads chromedriver
webdriver-manager==4.0.1

# OpenAI Python SDK (kept as fallback)
openai>=2.23.0

# Groq SDK — FREE tier, Llama 3.3 70B (replaces OpenAI/Gemini)
groq>=1.0.0

# HTTP requests — used for LeetCode GraphQL API
requests>=2.31.0
